image: node:latest
   
pipelines:
  pull-requests:
      release/deploy:
          - step:
              name: test pipeline
              script:
                  - echo "Hello World "$HEROKU_API_KEY
  
