image: node:latest
   
pipelines:
  branches:
      release/deploy:
          - step:
              name: test pipeline
              deployment: test
              script:
                  - echo $HEROKU_API_KEY
  
  
