image: node:latest
   
pipelines:
  branches:
      release/deploy:
          - step:
              name: test pipeline
              script:
                  - echo "Hello World "$HEROKU_API_KEY
  
